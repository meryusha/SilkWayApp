<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="silkway.merey.silkwayapp">

    <uses-library android:name="com.google.android.maps" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".agent.activities.MainActivity" />
        <activity android:name=".agent.activities.LoginActivity" />
        <activity android:name=".agent.activities.SignUpActivity" />
        <activity
            android:name=".agent.activities.TourDetailsActivity"
            android:theme="@style/StyledIndicators" />
        <activity android:name=".agent.activities.AgentOfferActivity" />
        <activity android:name=".agent.activities.AddTourActivity" />
        <activity android:name=".agent.activities.TourEditActivity" />
        <activity android:name=".agent.activities.AgentOfferEditActivity" />
        <activity android:name=".operator.activities.MainActivityOperator" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".operator.activities.OperatorToursFeed"></activity>
        <activity android:name=".operator.activities.TourDetailsActivityOperator"></activity>
    </application>

</manifest>